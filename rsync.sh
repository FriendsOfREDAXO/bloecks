#!/bin/bash
# bloecks asset sync script
# Since plugins have been eliminated and functionality moved to the main addon,
# this script is no longer needed but kept for compatibility.

echo "Asset sync complete - no plugins to sync (plugins eliminated in favor of integrated functionality)."
exit 0
